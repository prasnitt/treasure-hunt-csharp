namespace TreasurehuntApi.Data
{
    // All these codes will not be negative
    public enum SpecialCodes
    {
        // Finished all the check points
        FINISHED_CODE = 0,

        //// If state is not defined (game not started)
        //GAME_NOT_STARTED = -1,

        //// If Invalid team name given
        //TEAM_NAME_NOT_FOUND = -2,

    }
}
